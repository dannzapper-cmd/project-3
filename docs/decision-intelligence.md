# Decision Intelligence (PR-04)

## Scope

PR-04 adds an external decision intelligence layer on top of the PR-03 demand
forecasting pipeline. It does not modify InvenTree core and does not depend on
InvenTree internals. Inputs are the deterministic synthetic CSVs generated by
`make generate-data`.

## Run

```bash
make decision-intel
```

Equivalent:

```bash
MLFLOW_TRACKING_URI=mlruns MLFLOW_ALLOW_FILE_STORE=true \
  uv run --group ml python -m ml.decision_intelligence --config ml/config.yaml
```

## Artifacts

Generated local artifacts:

- `artifacts/decision/decision_recommendations.csv`
- `artifacts/decision/decision_summary.json`

The decision MLflow run logs:

- LightGBM quantile artifacts:
  - `lightgbm_quantile_p10`
  - `lightgbm_quantile_p50`
  - `lightgbm_quantile_p90`
- Decision artifacts under MLflow artifact path `decision/`
- Portfolio metrics:
  - `naive_total_cost`
  - `optimized_total_cost`
  - `cost_reduction_pct`
  - `selected_pinball_loss`

## Forecasting layer

The PR-03 training behavior is preserved. PR-04 trains separate LightGBM
quantile regressors for `alpha = 0.1, 0.5, 0.9` via `train_quantile_models()`.
These models are additive and do not replace `train_lightgbm()`.

Decision-layer quantile models exclude `lead_time_days` by default and validate
that stock, reorder, safety-stock, cost, and stockout fields are not used as
demand forecasting features. Inventory metadata is applied only after forecasts
are generated.

StatsForecast native intervals are attempted as an MLflow/reference summary
when available. LightGBM quantile intervals are the primary PR-04 interval MVP.

## Inventory formulas

### Safety stock

```text
safety_stock = z_score * demand_std * sqrt(lead_time_days)
```

Default `service_level = 0.95`; `z_score = scipy.stats.norm.ppf(service_level)`.

Assumption: lead time is fixed. The formula captures demand variance only, not
lead-time variance.

### Reorder point

```text
demand_during_lead_time = forecast_mean_daily * lead_time_days
reorder_point = demand_during_lead_time + safety_stock
```

### EOQ

```text
eoq = sqrt((2 * annual_demand * order_cost) / annual_holding_cost_per_unit)
```

Defaults in `ml/config.yaml`:

- `order_cost = 50.0` USD
- `annual_holding_cost_per_unit = unit_cost * 0.20`

Both are synthetic assumptions and are overridable in config.

## Stockout risk

Stockout risk uses a Normal approximation:

```text
mean_demand_lt = forecast_mean_daily * lead_time_days
std_demand_lt  = demand_std_daily * sqrt(lead_time_days)
P(stockout)    = 1 - norm.cdf(current_stock, mean_demand_lt, std_demand_lt)
```

Default thresholds:

- `low`: risk `< 0.20`
- `medium`: `0.20 <= risk <= 0.50`
- `high`: risk `> 0.50`

Thresholds are overridable in `ml/config.yaml`.

## Cost-aware MVP

The newsvendor quantile is:

```text
q = understock_cost / (understock_cost + overstock_cost)
```

Defaults:

- `understock_cost_per_unit = unit_cost * 1.0`
- `overstock_cost_per_unit = unit_cost * 0.20 / 365`

The pipeline selects the available LightGBM quantile closest to `q`, computes
pinball loss, and simulates holding plus stockout cost on the synthetic temporal
backtest. The naive baseline policy is `lag_7`.

Cost results are labeled as **synthetic simulated backtest only**. They are not
real-world savings or production claims.

## Output schema

`decision_recommendations.csv` contains one row per part with:

- `part_id`, `demand_pattern`
- `service_level`, `z_score`
- `lead_time_days`, `current_stock`, `unit_cost`
- `forecast_mean_daily`, `prediction_lower`, `prediction`, `prediction_upper`
- `demand_std_daily`, `demand_during_lead_time`
- `safety_stock`, `reorder_point`, `annual_demand`, `order_cost`
- `annual_holding_cost_per_unit`, `eoq`
- `stockout_risk`, `risk_level`
- `newsvendor_quantile`, `selected_quantile_alpha`
- `selected_quantile_forecast_daily`, `warnings`

## Limitations

- Synthetic data only.
- Simulated cost backtest only.
- No production savings claims.
- No dashboard/UI.
- No retraining loop, model registry, champion/challenger, Evidently, BentoML,
  observability stack, Kubernetes, or cloud deployment scope.
